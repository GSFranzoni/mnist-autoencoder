{
  "name": "mnist-autoencoder",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --continue",
    "lint": "turbo run lint",
    "lint:fix": "oxlint --fix .",
    "sync:weights": "bun packages/training/scripts/sync-weights.ts",
    "build:mnist-samples": "npm exec --yes --package=node@20.20.2 -- node ./packages/training/node_modules/.bin/tsx packages/training/scripts/build-samples.ts",
    "download:mnist": "bun packages/training/scripts/download-mnist.ts",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "start:testing": "bun run --filter @mnist-autoencoder/training test:mnist",
    "start:training": "bun run --filter @mnist-autoencoder/training train",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.20.2",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "oxfmt": "^0.67.0",
    "oxlint": "^1.79.0",
    "turbo": "^2.6.3",
    "typescript": "^7"
  },
  "packageManager": "bun@1.4.2"
}
